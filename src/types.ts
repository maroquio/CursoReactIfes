export type ItemCarrinho = {
    id: string;
    titulo: string;
    quantidade: number;
}

export type Product = {
    id: string;
    name: string;
    description: string;
    price: number;
    photo?: string;
}
